import { ComponentType } from "react";

type ArticlePropsType = {
    title: string;
    publishedAt: string;
    link: string;
    sample: string;
    imgName: string;
}

export default ArticlePropsType;